# 企业级航空知识图谱 RAG 系统 (KG-RAG) 深度缺项分析与演进规划

本文件基于对系统底层源码（解析引擎、检索路由、数据模型）的颗粒度审计，针对航空制造业严苛的工程标准，识别出系统在技术细节上的深层缺项。

---

## 1. 知识提取与解析细节 (Parsing & Extraction Granularity)

目前的解析逻辑（`parser.py`）依赖正则与基础字号识别，在面对高度复杂的工程文档时存在瓶颈。

- [ ] **语义化切片 (Semantic Chunking) 优化**：
    - **现状**：按固定章节号（1, 1.1）切片，忽略了工程文档中“步骤说明”与“注意事项”的逻辑连续性。
    - **建议**：引入基于布局分析（Layout Analysis）的切片，确保操作步骤（Procedure）与其前置条件（Prerequisite）在同一语境下。
- [ ] **工程公式与符号识别 (Formula & Symbol Support)**：
    - **现状**：无法识别 LaTeX 公式、化学分子式或材料牌号（如 $Ti-6Al-4V$）中的特殊下标。
    - **建议**：集成支持公式解析的 OCR 引擎（如 Nougat 或 Pix2Text），并在图谱中建立公式节点关系。
- [ ] **表格语义还原**：
    - **现状**：表格目前仅作为文本提取，丢失了行列索引关系，导致 LLM 难以回答“某材料在 250℃ 时的硬度值”这类查表问题。
    - **建议**：将表格转化为结构化 JSON 或 Markdown Table，并在图谱中建立“表头-表项”的层级索引。

## 2. 检索路由与策略深化 (Retrieval & Reasoning Depth)

目前的检索策略（`query.py`）仍处于粗粒度的向量/全文融合。

- [x] **基于领域词典的查询扩展 (Query Expansion)**：
    - **现状**：输入“钛合金”可能漏掉仅提及“TC4”或“Ti-6Al-4V”的章节。
    - **建议**：利用 Neo4j 中的 `SYNONYM_OF` 关系进行自动查询扩展，实现语义级的“所思即所得”。
- [ ] **可解释的引用溯源 (Precise Citation & Grounding)**：
    - **现状**：回答仅标注来源于“CPS1220 章节 3”，用户无法快速定位到 PDF 中的具体坐标。
    - **建议**：在入库时保存 PDF 的坐标（BBox）信息，前端实现“点击引用跳转至 PDF 高亮行”。
- [ ] **重排序模型 (Reranker) 的领域微调**：
    - **现状**：通用 BGE-Reranker 在处理航空专有名词时区分度有限。
    - **建议**：利用现有的 `QueryFeedback` 历史数据，进行对比学习（Contrastive Learning），微调垂直领域的重排序模型。

## 3. 知识生命周期与版本治理 (Knowledge Lifecycle)

- [ ] **增量更新与局部失效机制**：
    - **现状**：文档更新需全量重新处理，无法仅更新发生变动的某个小节。
    - **建议**：引入文档内容指纹（Hash），实现“段落级”的增量入库，自动断开旧关系的语义边并连接新节点。
- [ ] **多维关联发现 (Cross-Document Discovery)**：
    - **现状**：目前主要依赖显式引用。
    - **建议**：引入关联规则挖掘（Association Rule Mining），自动发现“两个从未互相引用的规范却在操作相同零件”的隐性联系。

## 4. 工业级运维与安全性 (Industrial Ops & Security)

- [ ] **知识图谱完整性约束 (Schema Invariants)**：
    - **现状**：`validator.py` 仅检查字段缺失。
    - **建议**：在 `lifespan` 启动时增加图谱完整性自检（如：检查是否有孤立的 Section 节点，或缺少 Document 根节点的 Section）。
- [ ] **导出审计与数据去标识化 (PII Masking)**：
    - **现状**：日志已脱敏，但导出数据仍含有编制人/审核人信息。
    - **建议**：在 `export_training_data` 接口中增加自动化去标识化逻辑，符合企业隐私合规。
- [ ] **离线化部署与显存优化**：
    - **现状**：多个本地模型（Embedding, Reranker, VLM）同时运行，显存压力巨大。
    - **建议**：引入模型量化（Quantization, 4/8-bit）与推理加速框架（如 TensorRT 或 vLLM），并支持 CPU/GPU 混合调度。

## 5. 前端交互与生产辅助 (UX & Productivity)

针对企业级用户（工艺员、质检员、现场工人）的实际作业流程，前端展示层可从以下维度深度增强：

- [ ] **多维图谱探索器 (Interactive Graph Explorer)**：
    - **建议**：不再局限于列表，提供基于 Force-Directed Layout 的全量/局部图谱视图。支持点击节点动态展开关系（Expand on Click），支持按关系类型（如 `REQUIRES_TOOL`）或节点属性进行筛选。
- [x] **PDF 深度锚点与高亮 (Deep Linking & Highlighting)**：
    - **建议**：实现从 LLM 回答的引用（Citation）直接跳转至 PDF 查看器的特定页码和坐标。利用 `pdf.js` 实现搜索词在原文中的自动高亮，并支持“原文-图谱-回答”的三屏联动。
- [ ] **工艺对比工作台 (Diff Workbench)**：
    - **建议**：不仅展示文本差异，还需在图谱层面可视化展示“版本变更路径”。例如，用红色标出 B 版中删除的材料节点，用绿色标出 A 版没有的新增工艺要求。
- [x] **语义搜索结果的聚类展示 (Search Clustering)**：
    - **建议**：当搜索“密封失败”时，前端自动按“原因”、“材料”、“工具”、“预防措施”对召回的几十个章节进行自动聚类展示，减少用户的阅读负荷。
- [x] **智能看板与治理驾驶舱 (Admin Dashboard)**：
    - **建议**：为管理员提供图谱健康度看板（孤立节点比例、关系密度）、用户查询热词云、以及待审核知识建议池。
- [x] **离线/移动端现场辅助 (Mobile Field Support)**：
    - **建议**：针对车间弱网环境，支持将特定文档及其图谱子集“离线缓存”。支持拍照搜索（拍摄零件条码或现场故障点），利用 VLM 自动关联至最近的工艺规范。
- [x] **协作式知识沉淀 (Collaborative Annotation)**：
    - **建议**：允许高级用户在前端直接为节点添加“现场心得”或“纠错备注”，这些标注可作为后续 LLM 微调的优质 RLFH 信号。



    1 ## 前端功能增强规划 (Frontend Roadmap)
    2
    3 ### Phase 1: 可解释性增强 (Explainability)
    4 - [ ] **双轴联动溯源系统**：实现 AI 回答与源文档 (PDF/Images) 的位置映射，点击引用直接跳转高亮。
    5 - [ ] **推理路径动画**：在图谱视图中动态高亮展示 RAG 检索时的多跳路径。
    6 - [ ] **多模态结果面板**：支持公式 (LaTeX) 和 工业图纸 (SVG/Image) 的原生渲染与标注。
    7
    8 ### Phase 2: 交互式知识管理 (Knowledge Management)
    9 - [ ] **可视化实体合并工具**：基于相似度计算结果，提供 UI 供管理员手动进行实体消歧与合并。
   10 - [ ] **图谱节点编辑器**：支持在前端直接增删改图谱节点和关系，并同步至 Neo4j。
   11 - [ ] **对话上下文知识捕获**：允许用户将对话中的新信息一键转化为知识图谱中的三元组。
   12
   13 ### Phase 3: 运维与监控 (Operations)
   14 - [ ] **数据处理看板**：实时展示文档解析、向量化及图谱构建的并行进度与错误堆栈。
   15 - [ ] **检索质量实验室**：提供对比视图，支持用户调整检索参数（如 Top-K, Alpha 权重）并实时观察结果变化。
   16 - [ ] **成本与性能监控**：前端展示单次查询的 Token 消耗、推理耗时及各组件 (ES, Neo4j, Milvus)
      的响应延迟。

    1 ### Phase 4: 深度洞察与分析 (Advanced Analytics)
    2 - [ ] **全局知识图谱热力图**：基于 PageRank 算法在前端展示核心节点，支持按业务领域聚类着色。
    3 - [ ] **动态 Schema 导航器**：可视化本体论（Ontology）结构，引导用户了解图谱覆盖的实体关联边界。
    4 - [ ] **时空演化视图**：针对有时间属性的节点，提供时间轴控件，展示知识随时间推移的构建过程。
    5
    6 ### Phase 5: 文档处理工作台 (Document Workbench)
    7 - [ ] **多模态手动补全**：为 OCR 或 视觉分析提供手动纠错界面，支持“框选-关联”操作直接建立实体联系。
    8 - [x] **PDF 智能锚点锚定 (Intelligent PDF Anchoring)**：实现 AI 回答与源文档 (PDF) 的位置映射。在 AI 回答中通过深度链接直接定位到 PDF 内部的具体章节或表格所在的页码。
    9 - [x] **批量任务处理面板 (Batch Task Processing Panel)**：支持对大规模文档入库任务的暂停、恢复、停止及实时审计日志展示。
   10
   11 ### Phase 6: 专家干预与调试 (Expert Feedback Loop)
   12 - [ ] **检索权重实验室**：动态调整向量权重 (Alpha) 和图权重，并支持保存不同的搜索策略预设。
   13 - [ ] **实体黑名单/白名单**：支持在 UI 上手动禁用某些错误实体，防止其进入 RAG 检索上下文。
   14 - [ ]
      **回答满意度精细化收集**：不仅是点赞/踩，支持用户对“检索准确度”、“生成流畅度”、“图谱关联度”进行多维度评
      分。

    
    ### Phase 7: 智能代理与自动化 (Agentic & Automation)
    - [x] **Agent 推理全链路追踪 (Reasoning Chain Tracing)**：可视化展示 LLM 决策路径（CoT）。在多跳推理模式下，实时流式展示问题分解、中间检索结果及综合过程。
    - [x] **工具箱状态监控器 (Toolbox Status Monitor)**：实时反馈向量数据库、图数据库及全文引擎的 I/O 状态、健康度及响应延迟。
    - [ ] **异常数据“巡检”看板**：自动高亮展示图谱中孤立的节点或结构异常的子图。

    5
    6 ### Phase 8: 时间维度与多维洞察 (Temporal & Multi-dim UX)
    7 - [ ] **知识演化时间轴**：通过滑块回溯图谱构建历史，支持对比不同时间点的数据增量。
    8 - [ ] **冲突知识仲裁界面**：可视化展示事实冲突（Fact Inconsistency），提供人工手动校准入口。
    9 - [ ] **地理/组织架构多维映射**：如果实体具有地理位置或部门属性，支持在地图或组织架构图中平滑切换视图。
   10
   ### Phase 9: 极客交互与开放能力 (Power User Features)
   - [x] **可视化 Cypher 构造器 (Visual Cypher Builder)**：通过拖拽实体和关系图标，自动生成复杂的图查询语句。支持实时预览生成的 Cypher 代码及结果集。
   - [x] **语义联想全域搜索 (Relationship-aware Autocomplete)**：搜索框支持关系路径联想。当锁定实体后，自动提示该实体在图谱中的外向关系，引导用户进行路径化检索。
   - [ ] **图谱预测“虚线”显示**：基于 GNN 预测潜在链路，支持用户一键“确认转实线”进行知识补全。
   - [ ] **知识资产一键导出**：支持将当前筛选的局部图谱导出为结构化文档 (JSON/Markdown/Excel)。